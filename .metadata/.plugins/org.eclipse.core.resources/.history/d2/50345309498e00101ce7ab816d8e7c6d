package WebdriverDemos;

import java.time.Duration;
import java.util.Iterator;
import java.util.Set;

import org.openqa.selenium.By;

public class sample1 extends baseFunction {
	
public static void main(String[] args) throws InterruptedException {
	
	
	launchBrowser("chrome"); // main | parent
	launchURL("https://www.naukri.com/");

	driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));

	// https://demo.guru99.com/test/upload/
			//launchURL("https://demoqa.com/automation-practice-form");
			 driver.navigate().to("https://demoqa.com/automation-practice-form"); // launch the URL;
			 driver.manage().timeouts().pageLoadTimeout(Duration.ofSeconds(30));
			 
			 driver.findElement(By.xpath("//*[@id='app']/div/div/div/div[1]/div/div/div[1]/span/div/div[1]")).click();
			driver.findElement(By.xpath("//*[@id='item-2']/span")).click();
			 
			  driver.navigate().refresh(); // refresh
			 driver.navigate().back(); // taking browser to previous url in single session
			 System.out.println("get title "+ driver.getTitle());
			 driver.navigate().forward(); // taking browser forward in single session.
			 System.out.println("get title "+ driver.getTitle());
}

}
