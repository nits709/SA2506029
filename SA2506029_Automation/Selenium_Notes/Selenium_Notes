Nitin Gupta.

GitHub link - https://github.com/nits709/SA2506029

Chromium based browser uses -- CDP (chrome dev protocol)
Safari - WebDriverAgent (WebKit)
Firefox - Marionette

Selenuim WebDriver Module no. 3
total session - 19 sessions.

1 . Content.
     - Concept of Automation.
     - Component of Selenium Webdriver.
     - Configuration of selenium webdriver.
     - Method of Webdrivers.
        1. Open Browsers (Google/Firefox)
        2. get() -> launch the URL.
        3. getCurrentURL();
        4. sendKeys() - (help to enter the data into text fields)
        5. click();
     - Locators in webdrivers.
        1. Name
        2. ID
        3. ClassName
        4. Xpath
        5. Css Selectors
        6. tagName
        7. LinkText
        8. partial Link
        9. Relative Locator. 

     - Handling Controls
         1. Button
         2. text/Box
         3. hyperLink
         4. radio Button
         5. Checkbox
         6. dropdown list
         7. fileupload
         8. WebTables

       Synchronization in Webdriver
        - Thread.sleep();
        - Implicit Wait
        - Explicit Wait
        - Fluent Wait  
        - PageLoadTimeout


       Handling Alerts
       Handling Multiple Windows
       Handling popups
       Mouse Action
           - Click
           - DobuleClick
           - right click
           - drag n drop
           - Mouse hover
       JavaScript executor
          - vertical scroll
          - horizontal scroll
       Screenshot
       
       testNG (unit testing framework)
        - Configuration
        - Execute single testcase
        - Multiple test
        - Parallel test (3-4)
        - generate the report
        - dataProvider.
        - Keyword framework
        - page object model framework
        - DataDriver framework.
        - Cucumber Approach
        - Hybrid framework.


        Java revision points.
         - Java Basics
            1. Variables
            2. Loops/Condition if else
            3. loops - for loop, enhance loop, nested, while.
            4. Arrays
            5. Method/functions
            6. Typecasting.
            7. Creating class and objects
                 - Constructor
                 - static variable and methods
                 - calling method with objects
            8. Ops -concepts.
            9. Exceptional Handling - throws keyword, try-catch
            10. Collections - list (*), set, map   



       What is software testing?
       Software testing is a process of identifying correctness, completeness, security and quality of development software (ccsq).

       software testing is a process to verifying and validating functionality in order to find defects
        d/w expected and actual results


        1. Scenario Creation
        2. testcase creation
        3. testdata creation
        4. RTM -> 
        5. Execution.

        	create gmail account
        	1. Open the browser
        	2. Enter URL
        	3. Enter Data
        	4. Select value
        	5. click on next
        	6. Enter DOB
        	7. Observe the result
        	8. mark my testcase pass/fail

        	Automation testing ?
        	above metions steps can be performed with help of machine/ automation tool.

        	Instructions need to be provided into a specific langugage (Java, python, ruby,perl, PHP, C# etc.).

        	Advantages of automation testing.
        	1. Faster
        	2. time saving
        	3. Cost (condtional)
        	4. Less Human efforts
        	5. Accuarcy
        	6. Cross browser testing
        	7. regression testing
        	8. Re-usability.
        	9. More test coverage - Cross browser testing | Mobile devices | Ipad | tab 
        	10. boost productivity.

        	When to Automate?
        	1. Stable - requirement | functionality
        	2. Long term projects duration.
        	3. Regression testing
        	4. Smoke / Sanity (conditional) | development | staging | production

        	ecommerce - amazon E2E scenario | homepage, login, PLP, PDP, Checkout, Orderconfirmation

        	Process of Automation and tool selection?
        	1. Planning - requirement, finance, technology, robust (browser/programming langugage), training, resources 
        	2. tool selection



       Selenium - is a open source project.
                  is has very strong community.
                  is set or bundle of test automation tools.

        Components of selenium projects. 
        1. Selenium IDE - (records and playback mechanism)
        2. Selenium RC - remote control (Deprecated).
        3. selenium grid - (parallel execution, cross browser).
        4. selenium webdriver - 4.34.0        	

        Selenium recorder - 2004 | jason huggins
        selenium IDE - 2006 | japan |
        selenium RC - 2008
        selenium webdriver - 2012 | simon stewart

        	100 testcases - browser . google chrome/ firefox. safari

         qualitia, katalon, watir, etc.

         class can implements interface and extends class only
         interface extends interface, cannot implements
         Selenium Webdriver
         1. Automation testing tool for testing web based(browser/websites)  Application.
         2. Selenium webdriver is an interface.
         3. is an API (Application programm interface)
         4. 


         selenium with java (login)
         1. open browser - google/firefox/safari
         2. get -> launch the URL over browser
         3. enter the data into field
         4. click on login

         Langugage binding | java, python, PHP, perl , c#.. etc.
         API - rest | xml


        1. How selenium werb-driver works
         test scripts execution - we write a test scripts using a selenium client(java, python, etc.) in your choosen langugage 

        2. Command translations - The client library translate out test script into json format (protocal) w3c or json wired 

        3. HTTPS communication : These JSON commands sends over HTTP to the appropriate browser driver (CD,FD, SD, OD, ED).

        4. Driver - Browser interactions: The browser driver recivies the command from protocol to perform the specified action (click, dropdown, get, enter)

        5. Handling response: The browser response is sent back through the driver to the client library which then proceed with next steps in the scripts.


        Pre- requities of selenium webdriver.
        1. Operating system - minimum window - 10
        2. Any latest browser with updated version
        3. minimum java version - 11 (for checking java -version)
        4. windows laptop - window key + R - cmd then hit enter
           java -version | java --version
        5. Any Editor (eclipse or intellij).
        6. selenium jar   

        Configuration of selenium webdriver
        1. Create 2 seperate folder
           a. Yourname_seleniumWorkspace
           b. Selenium Jars Files

        2. Open selenium.dev webssites
        3. Click on downloads
        4. Launch Eclipse  
        5. setting workspace -> select the 1st folder
        6. Now create a new project
           1. File --> New java project



       Methods of WebDrivers
       1. Launch the browser : 
               Create an object of webdriver interface, browser specific class   chromedriver, safariDriver, FirefoxDriver, OperaDriver.
       2. driver.manage().window().maximize(); maximized the any browser opened by webdriver object
       3. Get(): use to launch the any URL over browser
       4. Close():close the browser window.
       5. getTitle(): which returns the title of page (String) 
       6. getCurrentURL(): Which returns the URL of current page (String)
       7. findElement() : Reads the control of the webpage, locates for the first occurences
       8. getText(): Reads the text of element over webpage (this function works only if text is present within html tags)(String)
       
       
       
       
       Common exceptions in Selenium
       1. InvalidArgumentException : Your URL is in wrong syntax  , url should always starts with
           (http:// || https://) 
           
       2. NoSuchElementException: Selenium is unable to locate the element over page
       		What are possible reasons
       		1. The value provide that might be wrong
       		2. Value of locator dynamic in nature
       		3. Synchronization issue.   
       		4. Element could be presented inside Iframe.  
		3. InvalidSelectorException: The value of locator is in wrong format.
		4. PageLoadTimeOutException: If targeted url is not getting loaded in given time period.
		5. ElementClickInterceptException: occur when targeted element gets hidden by another element.
										to overcome user javascript function arguments[0].click


		WebElement
		1. Every Control/Element on the webpage treated as webelement in selenium webdriver.
		2. WebElement is an interface.
		3. WebElement can hold any control on the web page.
		
		
		Locators: is addresses/medium through which selenium can reach on them over page.
		1. Name
		2. Id - 
		3. ClassName : only one attribute value can pass at a time
		4. CssSelector
		5. Xpath
		6. Partialtext
		7. Linktext
		8. Tagname
		9. Relative

		CSSSelector : This is locators which help selenium to
					  reads the element by using 1 or multiple attributes
					 format --> tagname[attribute='value'] 
		types:
		 	1. Single Attribtue : tagname[attribute='value']
		 	    example: facebook | GMAIL -> input[name='firstName']
		 	    1. input[id='email']
		 	    2. input[name='pass']
		 	
		 	2. Multiple Attributes :  tagname[attribute='value'][attribute='value']
		 		example facebook
		 		1. input[id='email'][data-testid='royal-email']
		 		2. input[type='password'][id='pass']
		 	    
		 	3.Special characters 
		 		1. ^ - Starts with input[id^='email_']
		 		2. $ - Ends with
		 		3. * - contains
		 		4. . (Homework)
		 		5  # (Homework)   
		 	    
		 						  
		format CSS - tagname[attribute='value']
		
		special character will gonna applicable only on attribute value
		
		input[id='email_0123'] (if ending part of value is dynamic in nature)
		
	Notes: for css | user need to pass fully qualified attribute name
		   input[class='inputtext _55r1 _6luy _9npi']


	**How selenium webdriver reads the element over webpage
	1. selenium reads the element from left to right
	2. and line by line 
	
	
	Handling Dropdown
	1. If the element have select tagname then only then selenium treated as dropdown.
	2. For handling a dropdown selenium having a special class is called select class.
	3. BootStraps Dropdrown cannot automate via Select class.
	
	What all operations i can do with select class or dropdown.
	1. display the total no. of city from DD - 248 value
	2. Display firstSelectedOption (default value)
	3. Display name of each options
	4. Select the option (value from dropdown)
	
	Select class
		1. getFirstSelectedOption - return the default value of dropdown. (WebElement)
		2. getOptions - returns all options from dd (WebElement)
		3. how many way can select value from DD using select class
		   1. By using visible Text (text present in between the opening and closing tags)
		   2. By using value  (attribtue this could be a number or string)
		   3. By using index
	
	
	
	Synchronization (Waits in selenium)
	Synchronization is the process of adjusting the speed of tool with speed of application.
	
	
	Methods to use incase of synchronization issues.
	1. Thread.sleep();
			a. pauses the entire execution for give time period.
			b. it is applicable for single statement only.
			c. Mandatory delay (it takes mandatory delay 2,3,)
	2. implicit wait (dynamic waits)
			a. It doesn't take mandatory delay (if elements loaded with 2 second then it will not wait till the time is completed.)
			b. default polling interval: 500 ms .5 seconds
			c. Only checks the existence of an element in DOM (present in DOM)
			d. Applies : driver.findElement and driver.findElements
			e. Stops early : yes, if element founds before timeout. 
			f. Exception Ignored in give time period : NOSuchElementException. 	
	3. ExplicitWait (WebDriver Wait)
			a. It doesn't take mandatory delay (if elements loaded with 2 second then it will not wait till the time is completed.)
			b. default polling interval: 500 ms .5 seconds
			c. it checks the existence along with condition (state of an element) use by tester
	4. FluentWait
			
	5. PageLoadTimeOut


	//*[@id="root"]/div[3]/div/div[2]/div/div/div[1]/div[1]/div/div[2]/div[4]/div[1]/div[5]/div[1]/div/div[1]

	//*[@id="root"]/div[3]/div/div[2]/div/div/div[1]/div[1]/div/div[2]/div[4]/div[1]/div[7]/div[1]/div/div[1]


	relative xpath - //tagname[@attribute='value']
	this would return 1column value of 1st row ----> //table[@class='dataTable']/tbody/tr/td/a
	this would return me total no. of rows in webtable -> //table[@class='dataTable']/tbody/tr - 331
	this would return me total no. of column present inside each row --> //table[@class='dataTable']/tbody/tr[1]/td - 6


    Alert in selenium 
    Alert is an interface in the selenium webdriver (special class of webdriver)
    1. Alert is part of the browser
    2. until user input, user cannot access rest of the application.
    3. type of alerts
    		1. simple  --> only accepts the alert -- by clicking on ok button
    		2. confirmation
    		3. prompt (Input box/ normal)    		
    driver - > browser --> application --> click (Element) --> alert 
    		
    4. How to automate alert in selenium .
    		a. Methods of alert interface.
    			1. switchto();
    			2. accept();  -- for ok button
    			3. dismiss(); -- for cancel button
    			4. getText() -- return the text present on alert (String)
    			5. sendKeys()  allows user to enter text into prompt alert.	
    			
    
    javascriptExecutor : javascriptExecutor is an interface in selenium.
    					This accepts 2 parameter 1. method name, 2 element	
    					
    					
    Iframe:
    	1. Website -> iframe  -> element
    		Driver -> URL -> switchTo -> iframe -> element
    	
    	2. Website --> 
    			1. Iframe - 1  -> Element -1
    			2. Iframe - 2--> Element - 2
    		
    	Driver -> URl > SwitchTo ->  Iframe1 -> Element1	
    	go back to main page --> SwitchTo ->  Iframe2 -> Element2
    	
    	
    	3. Website -->  ParentIframe1  -> Element --> Child1Iframe --> Element
    					  --> Child2Iframe --> Element 
    					  
    					  
    					  
    					  
    					  Webdriver driver = new ChromDriver();
    					  
    					  
    					  
    					  
    					  		Webdriver -- interface
    					  		
    					  		
    					  	driver = new Chromdriver - class
    					  		
    					  		
    		javascriptExecutor js = javascriptExecutor (driver)
    		
    		
    		TakeScreenShot
    		
    		
    		Hospital is interface
    			1. blood test()
    			2. storeData()
    			3. registeration()
    			
    	
    	CityHospital implements Hospitals

    		4. fullbodyscan()
    		5. Kidneyscantest()	
    		
    		
    
    		GovtHospital implements Hospitals
    			
    		    4. fullbodyscan() 
    			
   
   how to work with properties files?
   1.  	Create an object of file class
   2. 	supply the filepath to this class
   3.  Whenever we reading data from external file -- java will read such data in form of streams
   		stream is channel in which data flows
   4. Create an object of fileInputStream / file outputstream
   5. Create an object of properties file
   6. sync properties with fileinput stream by using load();	
    			
    			
    			
    			
Location to download Apache - POI jars    			
1. Navigate to https://archive.apache.org/dist/poi/release/bin/
2. look up for poi-bin-5.2.3-20220909.zip (Jars)
3. Click on poi-bin-5.2.3-20220909.zip (jars will start download automatically)


	DataDriven Scenario with ExcelFiles
    testscripts driven by testData
    
1.  reading the data from excel file to execute testcases/ functionality
2.  to Open multiple URL's and validate the title of pages then write url and title over excel file
3.  get the data (webTable - cricket scoreboard) match. write the same data into excel file.
4.  placing order in ecommerce websites and capture order details then write into excel files.


		perf. 2 types of operations with POI libraries.
		1. write into excel file
		2. read from the excel file     

	Excel -> workbook
	
	workbook --> sheet -> Rows - Cells /Columns
	
		1. Create an object File.
		2. Create an object FileInputStream(Read)/FileOutputStream (Write)
		3. Create an Object of Workbook.
		4. Create a reference of Sheet.
		5. Create a reference of Rows
		6. Create a reference of cell/column
		
		
		1. File --> FileInputStream --> Workbook --> sheet -- rows -- cells
		
		2. File --> FileOutPutStream --> Workbook --> sheet -- rows -- cells


		type of excel file extension.
		1. XXXXX.xls  excel file version between from 97 - 2003
		2. .xlsx after MS office 2007
		
		
		.xls HSSFworkbook | outdated now (no one is using this).
		.xlsx XSSFworkbook | currently in use everywhere.
		
		
		What is Maven? depends on internet.
   Maven is build automation and project management tools
   no need to download the jars(selenium, POI,testng) Maven fetch them automatically.
   In project each testers using single POM. xml file.
   project object model.xml
    dependencies 
    	selenium 
    	testng
    	POI
    	Extentreport
   selenium jars
   testng jars
   POI jars 
   
   Maven having one central repository which hosted over internet. 	
   
   
   1000 tc's 
   i want to run this without jenkins
   run we can execute this with help of maven
   
   we can also execute this with help of jenkins + maven
   
   GroupID - ProjectName
   ArtifactsID  - PackageName
   
   Steps to create Maven Project.
   1. 
		
    
    
    BDD - Behavioral Driven Development
    BA,Tester,Developer, Sometime designer will gather the requirement (BRS/TRS)
    Requirements will be shared with QA team and QA team will create Feature File			
 	(First Component of BDD) (Collection of test scenario's)
 	Then this feature file will be shared with
 		1. Developer - so that developer create the application.
 		2. Manual Tester - so that manual tester can create a testcases.
 		3. Automation Tester - so that he/she will create automation scripts.
 
 	Now automation tester will create automation scripts - Glue code/ Step definition
 	(Second Component).
 	For execution of step definition (Automation scripts) tester have to use Runner class
 	(Third Component of BDD)
 	
 	Cucumber is used to implement BDD framework (approach)
 	
 	
 	Create cucumber project  (1st apporach)
 	1. File --> New --> Maven Project
 	2. Select 3rd check box --> next button
 	3. type "io.cucumber" as filter --> next
 	4. Enter archtypeID and groupID --> Finish
 	
 	5. In pom.xml select line number from 12
 		<properties> to </dependencies> (line no. 67)
 	6. then hit delete button from keyboard or backspace	
 	7. paste below code in pom.xml in line from 12 to 67
 		<properties>
				<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
				 <java.version>11</java.version>
				<junit.version>4.13.1</junit.version>
				<cucumber.version>6.8.2</cucumber.version>
				<cucumber.version>6.9.0</cucumber.version>
			</properties>
		<dependencies>
			<!--  https://mvnrepository.com/artifact/org.seleniumhq.selenium/selenium-java  -->
			<dependency>
			<groupId>org.seleniumhq.selenium</groupId>
			<artifactId>selenium-java</artifactId>
			<version>4.28.1</version>
			</dependency>
			<dependency>	
			<groupId>io.cucumber</groupId>
<artifactId>cucumber-java</artifactId>
<version>${cucumber.version}</version>
<scope>test</scope>
</dependency>
<dependency>
<groupId>io.cucumber</groupId>
<artifactId>cucumber-junit</artifactId>
<version>${cucumber.version}</version>
<scope>test</scope>
</dependency>
<dependency>
<groupId>junit</groupId>
<artifactId>junit</artifactId>
<version>${junit.version}</version>
<scope>test</scope>
</dependency>
</dependencies>
 			
 		
 		2. create cucumber project with second Approach
 		1. File -> new --> Maven project
 		2. Select 1st checkbox --> next
 		3. enter groupID and archtypeID --> next and finish.
 		4. paste line no.7 code (from approach 1st) after version tag till before </project>
 	
 	
 	
8. go to the eclipse marketplace under help
9. download cucumber plugins --> cucumber eclipse plugin 3.0

first component of BDD
1. Feature File. (Test suite)
		a. Collection of test scenario's
		b. Create a test scenario/ testcases by using gherkin langugage.
		c. file must have ending with .feature (extension) (downloaded cucumber eclipse plugin)
		d. Contains Keywords:
				Feature: Represent requirement
				Scenario: Test objective.
				Given: pre-conditions
				When: steps to follow (test steps)
				And: add multiple test steps
				Then: to check the expected condition
				Examples:
				Scenario Outline:
				
2. Second Component of BDD (StepDefinition files) Glue Code
		a. Normal java file
		b. have methods which satsified the steps metioned in feature file
		c. cucmber methods (Given,When,Then,Add,)
		d. webdriver code (logic)
			
		first scenario:
		To validate google search
		1. Open Google
		2. Read Title
		3. Validate the Title.	
		
		
		
		
		principle working in cucumber
		1. feature file can be created under src/test/resources
		2. feature --> StepsDefinition
			
				
 		======================
 		user below cucumber project depenedcies if u facing executino exceptions
 		<properties>
    <java.version>21</java.version>
    <selenium.version>4.35.0</selenium.version>
    <cucumber.version>7.29.0</cucumber.version>
    <junit.version>4.13.2</junit.version>
</properties>

<dependencies>
    <!-- Selenium -->
    <dependency>
        <groupId>org.seleniumhq.selenium</groupId>
        <artifactId>selenium-java</artifactId>
        <version>${selenium.version}</version>
    </dependency>

    <!-- Cucumber (Java) -->
    <dependency>
        <groupId>io.cucumber</groupId>
        <artifactId>cucumber-java</artifactId>
        <version>${cucumber.version}</version>
        <scope>test</scope>
    </dependency>

    <!-- Cucumber JUnit 4 integration -->
    <dependency>
        <groupId>io.cucumber</groupId>
        <artifactId>cucumber-junit</artifactId>
        <version>${cucumber.version}</version>
        <scope>test</scope>
    </dependency>

    <!-- JUnit 4 -->
    <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
    </dependency>
</dependencies>
		
 		
 	Tags in cucumber
 	1. tags is use to run/ execute testcases (scenario's ) in feature file
 	2. tags helps to group or filter the scenarios
 	3. With tags
 			a. run single testcase
 			b. run multiple testcases
 			c. skip single/multiple testcases
 	4. Tags defined in feature file only. (impact brings when user run scenarios from runner file).
 	5. @ - special character. @smoke, @sanity, @checkout, @login
 	6. tag conditions defined in runner file.			
 		
 		
 		
 		
 	How to get below mention scenario in Cucumber Hooks (anotations)
 	1. @Before similar like  BeforeMethod in testng
 	2. @After similar like  AfterMethod in testng
 	run some line of code prior to my each test
 	run some line of code after my each test ran.
 	
 	
 	
 	
 	Javascript Executor scroll.
	1. Scroll from one position to another position
	2. scroll into the element (ElementScrollIntoView)
	3. scroll based on pixel (co-oridnates)		
    			
    	
    	
Can designed POM (Page object Model) in 2 different way

create POM framework with 
PageFactory class -- from selenium 
without pagefactory class




Framework   
 ecommerce - 10 different project incompanies
 payment 
 
 Lakme company 
 
 Shiseido
 	NARS
 	DrunkElelphant
 	EMEA
 	
 	Disney - VPN
 	addidas - VPN
 	
 	
 	Framework  | main/src/java | Key feature of any automation framework
 	1. resuable feature
 		a. Webdriver code
 		b. utility
 			1. database
 			2. excelfile
 			3. propertiesfile
 			4. Email provide - Gmail, Outlook, yahoomail
 		C. Configuration
 			 1. run test on browser
 			 2. Report - extent Report /allure report
 			 
 	2. AutomationExcercise - Login functionality | src/test/java 
 			testcases
 			TestData
 			testProperties
 			ExcelData
 			testng.xml
 			extentreport
 			
 		Disney			 		
 	

	hybrid Framework
	1. Create maven project
	2. Dependencies into pom.xml
	3. Automation login functionality of automatino excercise
		cucumber + POM (page object model)
	4. Utility
		1. BaseFunction | Webdriver resuable function
							 webdriver.get()
							 sendkeys
							 click
							 getElement
					
			properties
			report
			screenshot				
					
							 	
    			
    			
    			
    										
















          












